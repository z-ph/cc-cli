# v2.0.2

新增 `--base` 参数支持，扩展 base 配置编辑能力。

## 新增

- `cc add` / `cc edit` 命令支持 `--base` 参数，可直接编辑 `base` 配置节
- `cc parse` 命令支持 `--base` 参数，可将配置写入 `base` 节
- 添加 Settings 合并与优先级说明文档

## 修复

- 修复 launch 测试在 Windows 上的 spawn 断言问题
